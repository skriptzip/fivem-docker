# Ignore everything except config folder and necessary files
*
!config/
!Dockerfile
!docker-compose.yml
!README.md

# But ignore these files in config folder
config/.git*
config/node_modules/
config/*.log